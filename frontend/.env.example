# Leave empty to use the Vite proxy (http://localhost:5173 → http://127.0.0.1:8000).
# Or point at the API directly (requires CORS on the API for http://localhost:5173).
# VITE_API_BASE=http://127.0.0.1:8000
